
fixed-address {
    prefix: /closest
    fanout: single
    bias: closest
}

fixed-address {
    prefix: /unicast
    fanout: single
    bias: closest
}

fixed-address {
    prefix: /exclusive
    fanout: single
    bias: closest
}

fixed-address {
    prefix: /multicast
    fanout: multiple
}

fixed-address {
    prefix: /broadcast
    fanout: multiple
}

fixed-address {
    prefix: /
    fanout: multiple
}
